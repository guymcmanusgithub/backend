const express = require('express');
const router = express.Router();
router.use(express.json());


// router.use('/', require('./public')); 

// router.use('/', require('./public/auth')); 
// router.use('/', require('./public/payments')); 

// router.use('/', require('./protected/user/cards'));
// router.use('/', require('./protected/user/orders'));
// router.use('/', require('./protected/user/userDetails'));
// router.use('/', require('./protected/user/subscriptions'));

// router.use('/', require('./protected/admin/users'));
// router.use('/', require('./protected/admin/cocktails'));


module.exports = router;